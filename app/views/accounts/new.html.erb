<h1>New Account</h1>

<%= link_to 'Back', 'index' %>

<%= form_for :account, :url => {:action => 'create', :user_id => @account.user.id} do |f| %>

    <%= render :partial => "form", :locals => {:f => f} %>

    <div class="form-buttons">
      <%= submit_tag "Create User" %>
    </div>

<% end %>