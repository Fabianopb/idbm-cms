<%= form_for(@refund_claim) do |f| %>
  <% if @refund_claim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@refund_claim.errors.count, "error") %> prohibited this refund_claim from being saved:</h2>

      <ul>
      <% @refund_claim.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
