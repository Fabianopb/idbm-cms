<h1>New User</h1>

<%= link_to 'Back to travel plans', {:action => 'index', :user_id => @user.id} %>

<%= form_for :travel_plan, :url => {:action => 'create', :user_id => @user.id} do |f| %>

    <%= render :partial => "form", :locals => {:f => f} %>

    <div class="form-buttons">
      <%= submit_tag "Create Travel Plan" %>
    </div>

<% end %>